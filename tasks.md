# Tasks

- [x] Add strict TDD traceability artifacts (`strict-tdd.md`, `tasks.md`, `apply-progress.md`).
- [x] Restore spec-aligned architecture modules under `simulation`, `vehicle`, `tire`, `circuit`, and `models/telemetry.py`.
- [x] Repair `SimulationService` so it uses real orchestration only.
- [x] Route routers through service classes and provide MVP behavior for `/scenarios`, `/simulations`, and `/reports/latest`.
- [x] Add error-path and runtime scaffolding tests, including 422 and 404 flows.
- [x] Fix Kubernetes manifest drift and remove obsolete conflicting deployments.
- [x] Add `pytest-cov` support and verify the coverage command.
- [x] Fix `.engram/config.json` project key.
